It is a good idea to set user defaults:
```
qmk config user.keyboard=crkbd user.keymap=zenzike
```
Then in order to flash, simply use:
```
qmk flash
```
This should be done for each side of the keyboard.

